from setuptools import setup, find_packages

setup(
    name='health-service-rest',
    version='1.0.0',
    packages=find_packages(),
    url='',
    license='',
    author='rosantos',
    author_email='',
    description='',
    setup_requires=["pytest-runner"],
    tests_require=["pytest"],
)
# packages=['src.app', 'tests', 'tests.app'],
